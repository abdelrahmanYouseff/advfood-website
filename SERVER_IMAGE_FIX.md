# إصلاح صور المطاعم على السيرفر

إذا كانت صورة الغلاف (أو أي صور أخرى) تظهر مكسورة على السيرفر، تحقق من التالي:

## 1. إنشاء رابط التخزين (مهم جداً)

الصور المرفوعة من لوحة التحكم تُحفظ في `storage/app/public/`. لعرضها يجب إنشاء رابط رمزي:

```bash
php artisan storage:link
```

هذا الأمر ينشئ `public/storage` كرابط لـ `storage/app/public`، وبالتالي تصبح الصور متاحة عبر المسار `/storage/restaurants/covers/...`

## 2. التحقق من ملف .env

تأكد من إعداد `APP_URL` بشكل صحيح:

```env
APP_URL=https://adv-line.sa
```

**مهم:** استخدم `https://` إذا كان الموقع يعمل على HTTPS.

## 3. صلاحيات المجلدات

تأكد من صلاحيات الكتابة:

```bash
chmod -R 775 storage bootstrap/cache
chown -R www-data:www-data storage bootstrap/cache
```

(استبدل `www-data` بمستخدم خادم الويب حسب إعدادك)

## 4. التحقق من وجود الملف

بعد رفع صورة جديدة، تأكد من وجودها:

```bash
ls -la storage/app/public/restaurants/covers/
```

## 5. التحقق من رابط الصورة

افتح أدوات المطور (F12) في المتصفح → تبويب Network، وحمّل صفحة المطعم. انقر على طلب الصورة الفاشل لمعرفة الرابط المستخدم والخطأ (404، 403، إلخ).
